# Environment and cleanup log

Updated: 2026-09-19

This file tracks non-source artifacts created for reconnaissance, builds, and visual verification. No credentials are stored here.

## Reference clones

All clones are shallow and ignored by Git. Remove `References/` to delete them.

- `References/listenbrainz-server`
- `References/listenbrainz-android`
- `References/listenbrainz-ios`
- `References/ListenBrainzKit`
- `References/firstfm-ios`
- `References/AppleMusicBottombarSwiftUI`
- `References/cassette`
- `References/Volta`
- `References/Minidisc`
- `References/Autohop`
- `References/Beans-Music`
- `References/bocan-music`
- `References/FastScrobbler`
- `References/finale`
- `References/discrobble`

## Installed applications and packages

- Xcode 27.0: installed at `/Applications/Xcode.app` through the existing Mac App Store CLI (`mas install 497799835`), then licensed and initialized with `xcodebuild -runFirstLaunch`. Cleanup: remove Xcode through Finder/App Store if it was installed solely for this project.
- iOS 27.0 Simulator runtime (arm64): downloaded with `xcodebuild -downloadPlatform iOS` for tests and visual checks (8.05 GB download). Cleanup: remove it from Xcode Settings > Components, or with the corresponding supported `simctl runtime delete` command after identifying the installed runtime.
- XcodeGen 2.44.1: used from an ephemeral `nix shell nixpkgs#xcodegen` environment. Nix downloaded XcodeGen and its Swift 5.10.1 runtime into the shared `/nix/store`; no profile package was installed. Cleanup is optional because the paths are content-addressed and may be shared; use normal Nix garbage collection rather than deleting store paths by hand.
- Zulu JDK 17.0.19: used from an ephemeral `nix shell nixpkgs#jdk17_headless` environment to exercise the official KMP iOS build. No profile package was installed; use normal Nix garbage collection for its shared `/nix/store` paths.
- Node.js 22 was used from an ephemeral `nix shell nixpkgs#nodejs_22` environment for the Year in Music website check. Playwright and its Chromium payload were installed only under `/tmp/listenbrainz-playwright-yim/`; no global npm package or browser was installed. Cleanup: remove that exact temporary directory, then use normal Nix garbage collection for shared `/nix/store` paths if desired.
- `ux-writing` skill from `content-designer/ux-writing-skill` (MIT) was installed at `/Users/nabeel/.codex/skills/ux-writing/` to audit user-facing text. Cleanup: remove that exact directory. It is development guidance only and is not linked into or shipped with Brainz.

## Generated caches and build output

- `References/ListenBrainzKit/.build/`: SwiftPM dependency/artifact cache generated by `swift test`. Cleanup: remove that exact directory.
- `ListenBrainzNative.xcodeproj/`: generated from tracked `project.yml` by XcodeGen. Cleanup: remove and regenerate it; it contains no unique source.
- `.derived-data/`: repository-local Xcode build products and indexes, currently generated by command-line build/test runs and ignored by Git. Cleanup: remove that exact directory.
- `/tmp/listenbrainz-xcodebuild.log`, `/tmp/listenbrainz-debug-build.log`, and `/tmp/listenbrainz-release-build.log`: transient command-line test and build logs. Cleanup: remove those exact files.
- `/tmp/listenbrainz-testbuild.log`: transient command-line test-build log. Cleanup: remove that exact file.
- `/tmp/listenbrainz-tests.log`, `/tmp/listenbrainz-package-tests.log`, `/tmp/listenbrainz-package-tests-current.log`, `/tmp/brainz-*.png`, `/tmp/brainz-*-boot.log`, and `/tmp/brainz-recommendations-network.log`: transient package/simulator test logs, visual checkpoints, and network timing evidence. Cleanup: remove those matching temporary files.
- `.derived-data/feed-backend/`, `.derived-data/feed-ui/`, `.derived-data/feed-tests-final/`, `.derived-data/feed-release-final/`, `.derived-data/feed-full-verification/`, and `.derived-data/feed-release-verification/`: feed implementation, test, visual-QA, and independent-verification artifacts. They are already covered by the repository-local `.derived-data/` cleanup rule above and are listed explicitly for provenance.
- `/tmp/brainz-feed-ui-build.log`, `/tmp/brainz-feed-final-focused-tests.log`, `/tmp/brainz-feed-final-all-tests.log`, `/tmp/brainz-feed-final-build.log`, `/tmp/brainz-feed-final-release-build.log`, `/tmp/brainz-feed-full-tests.log`, `/tmp/brainz-feed-release-build.log`, and `/tmp/brainz-feed-*.png`: transient feed build/test logs and visual checkpoints. Cleanup: remove those exact logs and matching screenshots. No package or application was installed for this milestone.
- `.derived-data/recommendation-feedback-tests/`, `.derived-data/recommendation-feedback-ui/`, `.derived-data/recommendation-feedback-all-tests/`, and `.derived-data/recommendation-feedback-release/`: focused tests, visual-QA builds, full-suite results, and the Release build for recommendation feedback. They are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/listenbrainz-package-tests-recommendation-feedback.log`, `/tmp/brainz-recommendation-feedback-focused-tests.log`, `/tmp/brainz-recommendation-feedback-all-tests.log`, `/tmp/brainz-recommendation-feedback-ui-build.log`, `/tmp/brainz-recommendation-feedback-release-build.log`, and `/tmp/brainz-recommendation-feedback-*.png`: transient package/app test logs, UI/Release build logs, and screenshots. Cleanup: remove those exact logs and matching screenshots. No package or application was installed for this milestone.
- `/tmp/listenbrainz-package-tests-social-mutations.log`, `/tmp/brainz-feed-mutation-build.log`, `/tmp/brainz-feed-mutation-focused-tests.log`, `/tmp/brainz-feed-fix-verification.log`, and `/tmp/brainz-feed-review-fix-tests.log`: transient typed social-mutation and feed-action test/build evidence. Cleanup: remove those exact files.
- `/tmp/brainz-social-actions-xcodegen.log`, `/tmp/brainz-social-actions-focused-tests.log`, `/tmp/brainz-recording-share-focused-tests.log`, `/tmp/brainz-social-actions-debug-build.log`, `/tmp/brainz-social-actions-visual-build.log`, `/tmp/brainz-social-actions-full-tests.log`, and `/tmp/brainz-social-actions-release-build.log`: transient project-generation, focused/full-test, and Debug/Release build logs for the social-actions milestone. Cleanup: remove those exact files.
- `/tmp/brainz-personal-recommendation-dark.png`, `/tmp/brainz-personal-recommendation-light.png`, `/tmp/brainz-personal-recommendation-axl.png`, and `/tmp/brainz-feed-hidden-dark.png`: transient light/dark/accessibility and hidden-event privacy screenshots. Cleanup: remove those exact files.
- `/tmp/brainz-release-detail-xcodegen.log`, `/tmp/brainz-release-detail-build.log`, `/tmp/brainz-release-detail-focused-tests.log`, `/tmp/brainz-release-detail-full-tests.log`, `/tmp/brainz-release-detail-visual-build.log`, and `/tmp/brainz-release-detail-release-build.log`: transient project-generation, test, Debug-visual, and Release-build evidence for the concrete-edition milestone. Cleanup: remove those exact files.
- `/tmp/brainz-mb-release.json`, `/tmp/brainz-mb-release-headers.txt`, `/tmp/brainz-mb-release-full.json`, and `/tmp/brainz-mb-release-full-headers.txt`: public MusicBrainz response/header samples used to verify the one-request edition payload and supported includes. They contain no credentials. Cleanup: remove those exact files.
- `/tmp/brainz-release-detail-dark.png`, `/tmp/brainz-release-detail-light.png`, `/tmp/brainz-release-detail-axl.png`, `/tmp/brainz-release-detail-axl-fixed.png`, `/tmp/brainz-release-detail-tracks-dark.png`, `/tmp/brainz-release-detail-tracks-dark-final.png`, `/tmp/brainz-release-detail-tracks-axl.png`, and `/tmp/brainz-release-detail-tracks-axl-fixed.png`: transient light/dark/accessibility simulator screenshots. `/tmp/brainz-release-detail-*-launch.log` contains transient launch output. Cleanup: remove these exact screenshots and matching launch logs.
- `/tmp/brainz-history-*.log` and `/tmp/brainz-history-*.png`: transient focused/full test logs, Debug/Release build logs, fixture launch output, and normal/dark/accessibility History screenshots. Cleanup: remove only these matching temporary files. The fixture used no ListenBrainz request or account token, and no package or host application was installed for this milestone.
- `.derived-data/delete-listen-*`, `/tmp/listenbrainzkit-delete-listen-*.log`, `/tmp/brainz-delete-listen-*.log`, and `/tmp/brainz-history-delete-*.png`: listen-deletion package/app tests, Debug/Release builds, result bundles, fixture launches, and visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use a local provider, make no production mutation, and use no real token.
- `.derived-data/request-gate-*`, `/tmp/request-gate-*.log`, and `/tmp/brainz-request-gate-*.log`: bounded/coalescing request-coordinator builds, focused tests, independent reruns, and result bundles. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Tests use local closures and fixture-only credential scopes, make no production request, and store no real token.
- `/tmp/lb-read-*.log`, `/tmp/provider-read-migration-*.xcresult`, and `.derived-data/lb-read-*`: provider read-migration builds, focused tests, and result bundles. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Tests use local transports and fixture-only credential scopes and make no production request.
- `.derived-data/credential-scope-*`, `/tmp/credential-scope-*`, `/tmp/lb-session-focused.log`, `/tmp/lb-cache-focused.log`, `/tmp/lb-cache-build.log`, and `/tmp/radio-cancel-repeat-*`: credential-scoped-cache/session hardening builds, focused/full tests, result bundles, and deterministic cancellation repetitions. Cleanup: remove only those exact/matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. The tests use injected stores/transports and fixture-only tokens, make no production request, and access no real Keychain credential.
- `.derived-data/daily-activity-focused/`, `.derived-data/daily-activity-full/`, and `.derived-data/daily-activity-release/`: focused tests, fixture builds, the full app suite, and the Release build for the listening-hours milestone. They are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/listenbrainz-package-tests-daily-activity.log`, `/tmp/brainz-daily-activity-*.log`, and `/tmp/brainz-daily-activity-*.png`: transient package/app test logs plus dark, light, and accessibility simulator screenshots for the UTC 7×24 heatmap. Cleanup: remove those exact/matching temporary files. The Debug fixtures made no production request and used no account token; no package or host application was installed for this milestone.
- `.derived-data/profile-playlists-*`: focused state/review tests, visual-QA builds, full-suite reruns, and Debug/Release builds for Profile playlists, including the cancellation-race verification directories and final clean 135-test checkpoint. These are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/listenbrainz-package-tests-profile-playlists.log`, `/tmp/brainz-profile-playlists-*.log`, and `/tmp/brainz-profile-playlists-*.png`: transient package/app test logs plus owned/collaborating light, dark, and accessibility simulator screenshots. Cleanup: remove those exact/matching temporary files. The fixture modes made no production request and used no real token; no new package or host application was installed, and XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/era-activity-*`: focused state tests, independent review runs, visual-QA builds, and final Debug/Release verification for Music by Decade, including `era-activity-review` and `era-activity-review-2`. These are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/listenbrainz-package-tests-era-activity.log`, `/tmp/brainz-era-activity-*.log`, and `/tmp/brainz-era-activity-*.png`: transient package/app test logs plus decade/year light, dark, and accessibility simulator screenshots. Cleanup: remove those exact/matching temporary files. The fixture modes made no production request and used no real token; no new package or host application was installed.
- `.derived-data/artist-evolution-*`: app tests, visual-QA builds, and final Debug/Release verification for Artist Evolution. These are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/listenbrainz-package-tests-artist-evolution.log`, `/tmp/brainz-artist-evolution-*.log`, and `/tmp/brainz-artist-evolution-*.png`: transient package/app test logs plus light, dark, and accessibility-size simulator screenshots. Cleanup: remove those exact/matching temporary files. The fixture mode made no production request and used no real token; no new package or host application was installed, and XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/year-in-music-*`: focused app tests, fixture builds, simulator products, visual-QA artifacts, independent review, full-suite verification, and Debug/Release builds for the current Year in Music slice. These are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/listenbrainz-package-tests-year-in-music.log`, `/tmp/listenbrainz-year-in-music-*.log`, and `/tmp/brainz-year-in-music-*.png`: transient package/app test logs and light, dark, focused-section, standalone-route, and accessibility simulator screenshots. Cleanup: remove those exact/matching temporary files. Fixture launches made no production request and used no real token.
- `.derived-data/year-in-music-art-*`, `/tmp/listenbrainz-package-tests-year-in-music-art.log`, `/tmp/brainz-year-in-music-art-*.log`, and `/tmp/brainz-year-in-music-art-*.png`: generated-art project generation, builds, focused/full tests, launches, logs, and light/dark/accessibility/smaller-device screenshots. Cleanup: remove those exact/matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches made no production request and used no real token. XcodeGen reused the recorded ephemeral Nix environment; no new package or host application was installed.
- `/tmp/year-in-music-art-release-build.log`: transient independent Release simulator-build log for the generated-art checkpoint. Cleanup: remove that exact file.
- `/tmp/lb-art-headers.txt`, `/tmp/lb-art.svg`, `/tmp/lb-art-204-headers.txt`, `/tmp/lb-art-204.svg`, and `/tmp/lb-art-rendered.png`: public art-endpoint reconnaissance and one local rendered preview. Cleanup: remove those exact files. The two production requests were separate and six seconds apart, exceeding ListenBrainz's one-request-per-second minimum. The existing `/tmp/listenbrainz-playwright-yim/` installation was reused; no new browser/package was installed.
- `.derived-data/genre-activity-*`: focused model/presentation tests, fixture builds, visual-QA products, independent review, full-suite verification, and Debug/Release builds for Genre Activity. These are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/brainz-genre-activity-*.log`, `/tmp/listenbrainz-genre-activity-*.log`, and `/tmp/brainz-genre-activity-*.png`: transient focused/full test logs, Debug/Release build logs, and light, dark, focused-ranking, and accessibility simulator screenshots. Cleanup: remove only those matching temporary files. Fixture modes made no production request and used no real token; XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/popularity-*`: focused provider/presentation tests, fixture builds, simulator products, full-suite verification, and Debug/Release builds for entity popularity. These are covered by the repository-local `.derived-data/` cleanup rule.
- `/tmp/brainz-popularity-*.log`, `/tmp/listenbrainz-popularity-*.log`, and `/tmp/brainz-popularity-*.png`: transient project-generation, package/public-smoke, app-test, launch, and light/dark/accessibility/smaller-device visual evidence. Cleanup: remove only those matching temporary files. The final fixture provider performs no production request and uses no real token; the separately recorded public smoke explicitly paces every ListenBrainz call above one second. No package or host application was installed for this milestone; XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/radio-*`, `/tmp/listenbrainz-package-tests-lb-radio.log`, `/tmp/brainz-radio-*.log`, and `/tmp/brainz-radio-*.png`: generated-radio focused/full tests, Debug/Release builds, simulator products, and light/dark/accessibility/smaller-device visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches made no ListenBrainz request and used no account token. No package or host application was installed; XcodeGen reused the recorded ephemeral Nix environment.
- `.derived-data/playlist-mutation-*`, `/tmp/listenbrainzkit-playlist-mutations-tests.log`, `/tmp/brainz-playlist-mutation-*.log`, `/tmp/brainz-playlist-edit-*.png`, and `/tmp/brainz-playlist-create-entry.png`: playlist create/edit project generation, package/app tests, Debug/Release builds, simulator products, and light/dark/accessibility/smaller-device visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches used local providers, made no production request, and used no real token. No package or host application was installed; XcodeGen reused the recorded ephemeral Nix environment.
- `.derived-data/playlist-append-*`, `/tmp/listenbrainzkit-playlist-append-tests.log`, `/tmp/brainz-playlist-append-*.log`, and `/tmp/brainz-playlist-add-*.png`: recording-to-playlist append package/app tests, Debug/Release builds, simulator products, and light/dark/accessibility/smaller-device visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use local providers, make no production request, and use no real token. No package or host application was installed for this milestone.
- `.derived-data/playlist-copy-*`, `/tmp/listenbrainzkit-playlist-copy-tests.log`, `/tmp/brainz-playlist-copy-*.log`, and `/tmp/brainz-playlist-copy-*.png`: playlist-duplication project generation, package/app tests, Debug/Release builds, simulator products, and visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use local providers, make no production request, and use no real token. No package or host application was installed; XcodeGen reused the recorded ephemeral Nix environment.
- `.derived-data/playlist-removal-build/`, `.derived-data/playlist-removal-tests/`, `.derived-data/playlist-remove-bridge/`, `.derived-data/playlist-remove-full-tests-final/`, `.derived-data/playlist-remove-full-tests-root/`, `.derived-data/playlist-remove-release/`, `.derived-data/playlist-remove-release-final/`, `.derived-data/playlist-remove-tests/`, `.derived-data/playlist-remove-tests-root/`, and `.derived-data/playlist-remove-visual-build/`: single-item removal Debug/Release builds, focused/full test results, independent review reruns, and fixture visual-QA products. `/tmp/listenbrainzkit-playlist-remove-*.log`, `/tmp/brainz-playlist-remove-*.log`, and `/tmp/brainz-playlist-remove-*.png` are the corresponding package/app logs and light, dark, smaller-device, and accessibility screenshots. Cleanup: remove only those exact directories and matching temporary artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture-only tests and visual routes use local providers, make no production request or mutation, and use no real token. No package or host application was installed; XcodeGen reused the recorded ephemeral Nix environment.
- `.derived-data/radio-save-tests/`, `.derived-data/radio-save-release/`, `.derived-data/radio-save-review-tests/`, `.derived-data/radio-save-review-tests-final/`, `.derived-data/radio-save-visual/`, `.derived-data/radio-save-full-tests/`, and `.derived-data/radio-save-release-final/`: focused and full simulator tests, independent hardening reruns, fixture visual-QA products, and Debug/Release build evidence for LB Radio private-playlist saving. `/tmp/brainz-radio-save-*.log` and `/tmp/brainz-radio-save-*.png` are the corresponding build/test logs plus final standard light/dark, accessibility-extra-extra-large, confirmation, and recovery screenshots. Cleanup: remove only those matching paths; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture transports and launches make no production request or mutation and use no real token. The initial onboarding-only screenshot is not acceptance evidence; a fixture-session correction enabled the later accepted checkpoints. XcodeGen reused the already-recorded ephemeral Nix environment; no package or host application was installed.
- `.derived-data/artist-origins-*`, `/tmp/brainz-artist-origins-*.log`, and `/tmp/brainz-artist-origins-*.png`: Artist Origins package/app tests, project generation, Debug/Release build products, and standard light/dark/accessibility/country-detail visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use a local provider, make no production request, and use no real token. No package or host application was installed; XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/artist-activity-*`, `/tmp/brainz-artist-activity-*.log`, `/tmp/brainz-artist-activity-*.xcresult`, and `/tmp/brainz-artist-activity-*.png`: Artist Activity package/app tests, project generation, Debug/Release build products, and standard light/dark/accessibility/expanded/smaller-device visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use a local provider, make no production request, and use no real token. The first onboarding-only screenshot is diagnostic rather than acceptance evidence; correcting the fixture-session route enabled the accepted checkpoints. No package, host application, or additional skill was installed; XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/top-listeners-*`, `.derived-data/full-suite-verify/`, `/tmp/brainz-top-listeners-*.log`, `/tmp/brainz-top-listeners-*.xcresult`, `/tmp/brainz-top-listeners-*.png`, `/tmp/brainz-full-suite-verify.log`, and `/tmp/brainz-full-suite-verify.xcresult`: entity Top Listeners project generation, package/app tests, Debug/Release builds, and standard light/dark/expanded/accessibility/smaller-device visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use a local provider, make no production request, and use no real token. The existing Computer Use skill was attempted for simulator interaction but installed nothing; a deterministic fixture route supplied the accepted screenshots. No package, host application, or additional skill was installed; XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/following-pins-*`, `/tmp/brainz-following-pins-*`, and `/tmp/following-pins-*`: Following Pins package/app tests, Debug/Release builds, result bundles, launch logs, and light/dark/accessibility/smaller-device/empty/failure screenshots. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture launches use a local provider, make no production request, and use no real token. The existing UX Writing skill guided user-facing copy. The configured in-app Browser skill was attempted for live inspection but had no available session and installed nothing; current API/server/frontend sources supplied the authoritative evidence. No package, host application, or additional skill was installed; XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/generic-art-*`, `/tmp/brainz-generic-art-*.png`, and `/tmp/brainz-generic-art-live-*.log`: Generic Art focused/full tests, Debug/Release builds, result bundles, local light/dark/accessibility/unavailable/failure screenshots, and one live public artist-art visual checkpoint. Cleanup: remove only those matching paths; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local providers and make no request. The live-only DEBUG hook was removed after one explicit token-free ListenBrainz artist-grid request; it loaded current Archive/CDN covers and Google fonts but performed no authenticated operation. One separate Archive `HEAD` redirect check was used during resource-policy reconnaissance. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy and XcodeGen reused the already-recorded ephemeral Nix environment.
- `/tmp/listenbrainz-playwright-yim/`: isolated npm project, Playwright package, and Chromium/browser payload used for live mobile website inspection. `/tmp/listenbrainz-yim-live-mobile.png`, `/tmp/listenbrainz-yim-live-mobile-full.png`, and `/tmp/listenbrainz-yim-live-mobile-top.png` are transient website screenshots. Cleanup: remove only those exact paths.
- The Debug Brainz app was installed into the disposable `Brainz QA iPhone 17 Pro` and `Brainz QA iPhone SE` simulators for fixture QA. Cleanup without deleting the simulators: run `xcrun simctl uninstall 'Brainz QA iPhone 17 Pro' dev.nabekhan.listenbrainznative` and the equivalent command for `Brainz QA iPhone SE`. No package or host application was installed for this milestone; XcodeGen reused the already-recorded ephemeral Nix environment.
- `~/Library/Developer/Xcode/DerivedData/ListenBrainzNative-*/`: Xcode's project-specific products, indexes, and test result bundles. Cleanup: remove only matching ListenBrainzNative directories after Xcode and Simulator are closed.
- `/tmp/brainz-xcodegen.log`, `/tmp/brainz-release-headers.txt`, `/tmp/brainz-release-response.txt`, and `/tmp/brainz-release-response.json`: transient project-generation and canonical-endpoint inspection evidence. Cleanup: remove those exact files.
- `~/.gradle/`: Gradle 9.4.1 wrapper distribution and dependency caches created while checking the official KMP module. This directory may be shared with other projects; inspect before removing or use Gradle's supported cache cleanup.
- `~/.konan/kotlin-native-prebuilt-macos-aarch64-2.3.10/` and `~/.konan/dependencies/`: Kotlin/Native compiler and LLVM/libffi dependencies downloaded by the official KMP build. These may be shared; inspect before removing.
- `References/listenbrainz-android/.gradle/`, `build/`, and `shared/build/`: ignored Gradle configuration/build output from the KMP framework experiment. Cleanup: remove only these exact directories under the reference clone, or remove the whole ignored reference clone.
- `.derived-data/artist-context-*`, `/tmp/brainz-artist-context-*.log`, and `/tmp/brainz-artist-context-*.png`: Artist Detail context focused/full tests, Debug/Release simulator builds, and populated/failure visual checkpoints in light, dark, accessibility, and smaller-device layouts. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local providers, make no production request, and use no real token. Both disposable QA simulators were shut down after acceptance. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy.
- `.derived-data/playlist-deletion-focused/`, `.derived-data/playlist-deletion-focused-final/`, `.derived-data/playlist-deletion-full/`, `.derived-data/playlist-deletion-full-final/`, `.derived-data/playlist-deletion-release-final/`, and `.derived-data/security-rereview/`: playlist-deletion focused/full tests, independent review, Debug/Release simulator products, and visual-QA builds. `/tmp/listenbrainzkit-playlist-delete*.log`, `/tmp/brainz-playlist-deletion-*.log`, and `/tmp/brainz-playlist-deletion-*.png` are the corresponding package/app test logs, XcodeGen/build/launch diagnostics, and accepted light, dark, recovery, confirmed, maximum-Dynamic-Type, and iPhone SE screenshots; `/tmp/brainz-playlist-deletion-xcodegen.log` is the project-generation log. Cleanup: remove only those exact directories and matching temporary files; repository-local derived data is also covered by the general `.derived-data/` rule. The initial blank screenshots were warm-up diagnostics, not acceptance evidence. Fixture routes use local providers, make no production request or mutation, and use no real token. Both disposable QA simulators were shut down after acceptance. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy and XcodeGen reused the recorded ephemeral Nix environment.

## System changes

- `.derived-data/fresh-releases-*`, `Packages/ListenBrainzKit/.build/`, `/tmp/fresh-releases-core-*.log`, `/tmp/listenbrainzkit-swift-test.log`, `/tmp/brainz-fresh-releases-*.log`, `/tmp/brainz-fresh-releases-*.png`, `/tmp/lbn-fresh-review-derived/`, and `/tmp/lbn-fresh-review-build-for-testing.log`: Fresh Releases query/filter implementation builds, focused app and package tests, independent review products, simulator launch/process logs, and diagnostic plus accepted light/dark/maximum-Dynamic-Type/iPhone SE screenshots. Cleanup: remove only these matching paths; repository-local derived data is also covered by the general `.derived-data/` rule. One focused worker command named a simulator that was not installed and stopped without launching it, one visual build caught a missing explicit `return` and was immediately superseded by a successful build, and early blank/truncated accessibility captures are diagnostics rather than acceptance evidence. The final focused app suite passes 10/10 and the vendored package suite passes 139/139; fixture routes use a local provider, suppress remote artwork, make no production request, and use no real token. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided visible and accessibility copy.
- `.derived-data/playlist-reorder-focused/`, `Packages/ListenBrainzKit/.build/`, `/tmp/brainz-reorder-build/`, `/tmp/brainz-reorder-*.log`, and `/tmp/brainz-reorder-*.png`: one-track playlist-reorder package/app builds, focused and full tests, result bundles, independent review reruns, and light/dark/recovery/accessibility/smaller-device screenshots. Cleanup: remove only these exact directories and matching temporary files; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes and visual providers are local, make no production request or mutation, and use no real token. No package, host application, or additional skill was installed; the existing UX Writing and Computer Use skills were used. Computer Use installed nothing and could not attach because this Xcode installation exposes no interactive Simulator host app. One earlier focused runner stalled in a scoped `simctl diagnose`; only its exact test/diagnostic process IDs were terminated, with no broad process cleanup. Both disposable QA simulators were shut down after acceptance.
- `.derived-data/user-artists-release/`, `/tmp/brainz-user-artists-*.log`, and `/tmp/brainz-user-defining-artists-*.png`: visited-user artist-navigation focused/full tests, Debug and universal Release simulator builds, and light/dark/maximum-Dynamic-Type/iPhone SE visual checkpoints. Cleanup: remove only those exact/matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. The fixture uses local values, makes no production request, and uses no real token. No package, host application, or additional skill was installed; the existing UX Writing skill guided the labels and accessibility hint.

- Active developer directory was changed system-wide to `/Applications/Xcode.app/Contents/Developer` with `xcode-select`. Cleanup/reversal: run `sudo xcode-select -s /Library/Developer/CommandLineTools` if full Xcode should no longer be the default.
- No credentials have been written to disk or shell scripts.
- During recovery from an interrupted turn, `/Users/nabeel/.Trash/ListenBrainzNative` was moved back to `/Users/nabeel/ListenBrainzNative`. No duplicate project copy remains in Trash.
- Disposable simulator devices named `Brainz QA iPhone 17 Pro` and `Brainz QA iPhone SE` were created on the iOS 27 runtime. Cleanup: `xcrun simctl delete 'Brainz QA iPhone 17 Pro'` and `xcrun simctl delete 'Brainz QA iPhone SE'`.
- `.derived-data/critiquebrainz-*`, `/tmp/brainz-critiquebrainz-*`, `/tmp/brainz-known-fixture.png`, and `/tmp/brainz-top-listeners-known.png`: CritiqueBrainz build/test products, an app-process diagnostic sample, and populated/dark/accessibility/failure visual-QA artifacts; the two known-fixture screenshots only isolated simulator launch behavior. The reader milestone adds focused/full/Release products plus summary-entry and full-text light, dark, maximum-Dynamic-Type, and iPhone SE screenshots under the same patterns. Cleanup: remove only those matching paths; repository-local derived data is covered by the general `.derived-data/` rule. Fixture routes and tests use local values/providers and make no request. One initial reader test command named an unavailable simulator and stopped before building; the UDID-targeted rerun passed. Exactly two intentional token-free public CritiqueBrainz requests were made during the original response-shape reconnaissance, and the reader added none. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy and the recorded XcodeGen environment was reused.
- `.derived-data/history-loaded-search*` and `/tmp/brainz-history-loaded-search-*`: focused/full app tests, Debug/Release simulator products, logs, and populated/empty light, dark, maximum-Dynamic-Type, and iPhone SE visual checkpoints for local loaded-listen search. Cleanup: remove only those matching paths; repository-local derived data is covered by the general `.derived-data/` rule. The first Pro and SE captures were warm-up diagnostics rather than acceptance evidence. Fixture launches use the existing local History provider, make no production request, and use no real token. One `open -a Simulator` attempt failed because this Xcode installation exposes no Simulator host app; it installed nothing, and `simctl` supplied the accepted checkpoints. Both disposable QA simulators were shut down after acceptance. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy.
- `.derived-data/inspect-listen-*`, `/tmp/brainz-inspect-listen-*.log`, and `/tmp/brainz-inspect-listen-*.png`: focused/full test products, project-generation or visual-QA logs, and mapped/unmapped light/dark/accessibility screenshots for the read-only Listen details sheet. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local data and make no production request. XcodeGen reused the recorded ephemeral Nix environment; no package, host application, or additional skill was installed.
- `.derived-data/connected-services-*`, `/tmp/brainz-connected-services-*.log`, and `/tmp/brainz-connected-services-*.png`: Connected Services focused app/Kit test products, project-generation logs, and populated/empty/failure light/dark/small-screen/accessibility fixture screenshots. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local providers, make no production request, and use no real token. XcodeGen reused the recorded ephemeral Nix environment; no package, host application, or additional skill was installed. Device Hub, which ships with the existing Xcode installation, was opened only while foregrounding simulator fixtures and then quit.
- `.derived-data/listen-submission-*`, `.derived-data/manual-submission-*`, `/tmp/brainz-submission-*.log`, `/tmp/brainz-listen-submission-*`, and `/tmp/brainz-manual-submission-*`: manual-listen focused/full tests, Debug/Release builds, result bundles, launch logs, and blank/prefilled/success/uncertain/rejected/recovery visual checkpoints across light, dark, accessibility, and smaller-device layouts. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local providers, make no production request or mutation, and use no real token. The disposable QA simulators were erased and reinstalled to remove stale state, then shut down after acceptance. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy and XcodeGen reused the recorded ephemeral Nix environment.
- `.derived-data/similar-artists-*` and `/tmp/brainz-similar-artists-*.png`: Similar Artists focused/full tests, Debug/Release and visual builds, result bundles, plus light, dark, expanded, maximum-accessibility, and smaller-device screenshots. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. The first onboarding, blank-launch, pre-fix accessibility, and failed-build screenshots are diagnostic rather than acceptance evidence. Fixture routes use a local provider, make no production request, and use no real token. Reconnaissance had already made exactly one intentional token-free public artist-page POST to confirm the production response shape; this milestone made no additional live call and retained no separate response artifact. No package, host application, or additional skill was installed; the existing UX Writing skill guided copy and XcodeGen reused the recorded ephemeral Nix environment.
- `.derived-data/legacy-yim-build/`, `.derived-data/legacy-yim-build-final*`, `.derived-data/legacy-yim-tests/`, `.derived-data/legacy-yim-focused*`, `.derived-data/legacy-yim-full-review/`, `.derived-data/legacy-yim-release*`, `/tmp/legacy-yim-*.log`, and `/tmp/brainz-legacy-yim-*.png`: archival Year in Music focused/full test products, Debug/Release builds, result bundles, and 2021/2024/current/artwork visual checkpoints in light, dark, maximum accessibility text, and the smaller iPhone SE layout. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local report/art providers and no real token. The two disposable QA simulators were shut down after acceptance. No production request, credential, package, host application, or additional skill was used; the existing UX Writing skill guided new copy.
- `.derived-data/external-links-*`, `/tmp/brainz-external-links-*.log`, and `/tmp/brainz-external-source-*.png`: external-source focused/full test products, Debug/Release builds, result bundles, fixture rebuilds, and light/dark/maximum-accessibility/smaller-device visual checkpoints. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. One initial onboarding render exposed and led to a DEBUG-only fixture-session routing fix; the accepted screenshots use local providers, make no production request, and use no real token. The two disposable QA simulators were reset and shut down after acceptance. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided copy, and the existing Computer Use skill was attempted for an accessibility scroll check but installed nothing and could not attach because this Xcode installation exposes no interactive Simulator host app.
- `.derived-data/artist-highlights-*`, `/tmp/brainz-artist-highlights-*.log`, `/tmp/brainz-artist-highlights-*.xcresult`, and `/tmp/brainz-artist-highlights-*.png`: Artist Highlights project generation, focused/full tests, Debug/Release builds, result bundles, launch logs, and Tracks/Releases/loading/failure visual checkpoints in light, dark, maximum accessibility text, and the smaller iPhone SE layout. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use a local provider, make no production request, and use no real token. Reconnaissance made exactly two intentional token-free public artist-page POST checks: an unavailable fixture identity returned 404, then a known artist confirmed the current combined schema; no response body was retained. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided copy and XcodeGen reused the already-recorded ephemeral Nix environment.
- `.derived-data/taste-identity-release/`, `/tmp/brainz-taste-identity-*.log`, and `/tmp/brainz-taste-identity-*.png`: Taste artist-identity Debug/full-test and universal Release simulator products, logs, and mixed mapped/unmapped Artist Evolution screenshots. Cleanup: remove only those matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. Fixture routes use local values, make no production request, and use no real token. Both disposable QA simulators were shut down after acceptance. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided the label and accessibility copy.
- `.derived-data/user-detail-albums/`, `.derived-data/user-profile-albums-*`, `/tmp/user-detail-albums-tests.log`, and `/tmp/brainz-user-profile-albums-*`: visited-profile album focused/full tests, Debug/Release builds, result bundles, and light/dark/maximum-Dynamic-Type/iPhone SE visual checkpoints. Cleanup: remove only those exact or matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. The accepted fixture uses local values and disables remote artwork, making no ListenBrainz, MusicBrainz, or Cover Art Archive request and using no real token. One earlier diagnostic screenshot allowed the mapped fixture through the normal Cover Art Archive loader before remote artwork was disabled; it made no ListenBrainz request and is not acceptance evidence. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided the section, state, and accessibility copy.
- `.derived-data/taste-release-groups-*`, `/tmp/brainz-taste-release-groups-*`, and `/tmp/Brainz_*.sample.txt`: Taste release-group focused/race/full tests, Debug and universal Release simulator products, result bundles, compiler/test logs, a scoped process sample from one deliberately interrupted hung regression test, and diagnostic plus accepted light/dark/maximum-Dynamic-Type/iPhone SE screenshots. Cleanup: remove only those exact or matching artifacts; repository-local derived data is also covered by the general `.derived-data/` rule. The hung test exposed a stale-cache early return; only that runner's exact `xcodebuild`, build-service, diagnostic, and app process tree was terminated before the corrected seven-test rerun passed. The first History and blank screenshots were fixture/warm-up diagnostics, and the first accessibility screenshot preceded the adaptive-row correction. Accepted fixture launches use local values and suppress artwork URLs; simulator log checks found no ListenBrainz, MusicBrainz, Cover Art Archive, or Archive.org host access, and no real token was used. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided visible and accessibility copy.
- `~/Library/Developer/Xcode/DerivedData/ListenBrainzNative-*/Logs/Test/Test-ListenBrainzNative-2026.09.19_17-01-28--0600.xcresult` and `Test-ListenBrainzNative-2026.09.19_17-03-10--0600.xcresult`: transient focused-test result bundles from overlapping or stalled local runners. Cleanup: remove only these exact result bundles with the normal project-specific DerivedData cleanup. Only the exact `xcodebuild`, test-app, and `simctl diagnose` process trees were stopped; no broad process cleanup occurred. No request, credential, package, or host application change was made.
- `.derived-data/profile-tracks-*` and `/tmp/brainz-profile-tracks-*`: Profile tracks focused/full test products, Debug and universal Release simulator builds, result bundles, compiler/test and host-inspection logs, plus diagnostic and accepted light/dark/maximum-Dynamic-Type/iPhone SE screenshots. Cleanup: remove only those matching paths; the repository-local derived-data cleanup rule also applies. One initial light screenshot was a warm-up blank and is not acceptance evidence. Earlier scoped runners stalled before test launch and only their exact `xcodebuild`/build-service or test-app process trees were terminated; the final focused suite passes 23/23, the full suite passes 537/537, and the final Release executable contains `x86_64` and `arm64`. The accepted fixture uses local values and suppresses artwork URLs; simulator log inspection found no ListenBrainz, MusicBrainz, Cover Art Archive, or Archive.org host access. Both disposable QA simulators were reset to light/large and shut down. No live request, credential, package, host application, or additional skill was installed; the existing UX Writing skill guided visible and accessibility copy.
- `.derived-data/home-tracks-*`, `/tmp/brainz-home-tracks-*`, and `/var/folders/7h/cvw0xr053v957sl_cvjs7jpc0000gn/T/ResultBundle_2026-19-09_17-46-0036.xcresult`: Home top-track Debug/full-test/Release products, compiler and process-inspection logs, screenshots, and the result bundle from one initial build command that used the product name instead of the project scheme and stopped before compilation. Cleanup: remove only these exact or matching paths; the repository-local derived-data cleanup rule also applies. The first onboarding and blank simulator captures are diagnostics rather than acceptance evidence. The accepted fixture uses local mapped/unmapped values and disables remote artwork; the final scoped process-log window contains no ListenBrainz, MusicBrainz, Cover Art Archive, Archive.org, or Spotify host activity. All 537 app tests pass and the final Release executable contains `x86_64` and `arm64`. Both disposable QA simulators were reset to light/large and shut down after acceptance. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided visible and accessibility copy.
- `.derived-data/ranked-route*` and `/tmp/brainz-ranked-route-*`: ranked-track identity Debug/full-test/Release products, result bundles, compiler and app stdout/stderr logs, the scoped simulator network log, and diagnostic plus accepted Taste/Artist/Year in Music light, dark, maximum-Dynamic-Type, and iPhone SE screenshots. Cleanup: remove only these matching paths; the repository-local derived-data cleanup rule also applies. The first Taste light screenshot loaded cached/remote cover art before the focused fixture disabled artwork, the first Artist iPhone SE screenshot captured launch before content appeared, and the first final Year in Music screenshot captured its slow simulator warm-up; all are diagnostics rather than acceptance evidence. Accepted fixtures use local mapped/unmapped values, make no live network read, disable track artwork where needed, and use no real token. One fresh full-suite attempt under `.derived-data/ranked-route-final-tests` was intentionally stopped by terminating only its exact runner process tree after broader review made its pre-Year-in-Music result obsolete; it carries no test verdict. The final focused Year in Music suite passes 10/10, the full app suite passes 537/537 with no failures or skips, and the final Release simulator binary contains `x86_64` and `arm64`. A scoped accepted-fixture log contains no ListenBrainz, MusicBrainz, Cover Art Archive, Archive.org, or Spotify service hostname. Both disposable QA simulators were reset to light/large and shut down after acceptance. No credential, package, host application, or additional skill was installed; the existing UX Writing skill guided the track terminology and accessibility copy.
- `.derived-data/yim-identity*` and `/tmp/brainz-yim-identity-*`: Year in Music identity-chapter focused/full test products, Debug and universal Release simulator builds, result bundles, compiler/test logs, architecture evidence, and diagnostic plus accepted light/dark/`.xxxLarge`/maximum-accessibility/iPhone SE screenshots. Cleanup: remove only these matching paths; the repository-local derived-data cleanup rule also applies. The first blank capture was a warm-up diagnostic and the first populated attempt reached onboarding because the new DEBUG argument had not yet been added to fixture session restoration; both were superseded after the route fix. Two attempts to open a Simulator host app failed because this Xcode installation does not expose one and installed nothing; `simctl` supplied the accepted evidence. The final focused suite passes 18/18, the full suite passes 543/543, and the Release executable contains `x86_64` and `arm64`. Accepted fixtures use a local report provider and no real token; scoped simulator logs contain no ListenBrainz, MusicBrainz, Cover Art Archive, Archive.org, or Spotify service hostname. Both disposable QA simulators were reset to light/large and shut down. No live request, credential, package, host application, or additional skill was installed; the existing UX Writing skill guided the visible and accessibility copy.
