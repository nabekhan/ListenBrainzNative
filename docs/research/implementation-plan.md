# Implementation plan

## Research verdict

Build a native SwiftUI application around a small app-owned domain/provider boundary. Start from the fixed ListenBrainzKit fork for P0, reuse current media interaction patterns selectively from MPL/MIT donors, and keep KMP replaceable rather than foundational. The app targets iOS 18 for broad modern API coverage, with iOS 26 enhancements (`tabViewBottomAccessory`, minimized tab bar, zoom transitions) behind availability checks.

## Architecture recommendation

```text
SwiftUI Features + shared Components
              ↓
@Observable feature models
              ↓
App-owned provider boundaries
  ├─ ListeningProvider → ListenBrainzProvider
  ├─ SearchProviding → ListenBrainz + MusicBrainz
  ├─ PinProviding → ListenBrainzKit pins extension
  ├─ SocialProviding → ListenBrainzKit social/core clients
  ├─ FeedProviding → ListenBrainzKit typed feed reads
  ├─ FollowingPinsProviding → ListenBrainzKit public aggregate pin pages
  ├─ RecordingShareProviding → typed follower/public/personal recommendation calls
  ├─ RecommendationsProviding → typed CF + generated-playlist reads
  ├─ ProfilePlaylistsProviding → typed owned/collaborator playlist pages
  ├─ PopularityProviding → typed global entity counts
  ├─ RadioProviding → typed LB Radio generation + one batch metadata lookup
  ├─ RadioPlaylistSaveProviding → typed private populated-playlist creation
  ├─ ReleaseDetailProviding/PlaylistDetailProviding → typed LB metadata/JSPF reads
  ├─ ConcreteReleaseDetailProviding → gated MusicBrainz edition/media lookup
  └─ Feature models → staged reads + bounded section/entity caches
              ↓
  ├─ fixed ListenBrainzKit core/metadata/stats/feedback
  ├─ focused endpoint extensions (only when Kit is missing them)
  └─ one process-wide ListenBrainz coordinator + response/metadata caches
              ↓
ListenBrainz / MusicBrainz / Cover Art Archive
```

- Organize concrete feature folders only as screens land: Authentication, Home, History, Recording, Artist, Profile, then Stats/Discover/Social/Playlists.
- UI never imports API response models directly. Stable app models preserve MBID/MSID/release/release-group distinctions and tolerate incomplete mapping.
- One process-shared actor admits at most two independent ListenBrainz reads, coalesces exact requests only within an opaque credential scope, cancels abandoned queued work, and applies server-directed 429 deferrals globally. Mutations remain serialized, paced to one start per second, and never retry automatically. MusicBrainz remains independently gated so unrelated hosts do not block one another. Small bounded caches supply stale-while-revalidate data without persisting raw HTTP responses.
- Keychain stores one versioned atomic username/token record. UserDefaults stores only an optional public-browsing username, and account transitions invalidate username-keyed disk snapshots before new credential or public state becomes observable. Raw tokens never enter defaults, logs, previews, request/cache keys, or telemetry.
- Native SwiftUI/Charts/NavigationStack/search/context menus/accessibility first. No third-party architecture framework.

## Vertical slice

1. Token onboarding, validation, Keychain persistence, and explicit demo/public-profile mode for previews.
2. Home: Playing Now or latest listen hero, recent shelf, short listening snapshot, and the shared current-pin surface.
3. History: date-grouped paginated listens, artwork, exact time/source, pull to refresh, loading/error/empty states, and server-bounded local-day navigation with adjacent-day controls.
4. Recording detail: identity, artwork, release/artist links, counts where available, love/hate/pin/recommend hooks.
5. Artist detail: artwork hero, personal count, top recordings and releases.
6. Profile: identity/listen count and top artists/releases/recordings.
7. Real API smoke tests, model fixtures for unmapped/multi-artist/missing-art data, simulator screenshots at two sizes and dark/light modes.

## Staging after the slice

- Phase 3: server-efficient full History/date navigation and safe single-listen deletion are now implemented alongside native recommendation feedback, My Feed/Following/Similar with supported feed mutations, public/personal recording sharing, Statistics with a UTC listening-hours heatmap, local-daypart Genre Activity, Music by Decade, Artist Evolution, Artist Origins, and Artist Activity, Fresh Releases, scoped search, Pins and Following Pins, the shared Home current-pin shelf, visited-user/social profiles, lazy owned/collaborating Profile playlists, release-group/playlist details, authenticated empty-playlist creation, owner metadata/privacy editing, safe one-recording playlist append, safe visible-playlist duplication, safe single-item playlist removal, safe one-track playlist reordering, safe creator-only playlist deletion, For You recommendations, global entity popularity, artist/release-group Top Listeners, and a guarded native Similar Artists shelf. Multi-item playlist changes remain staged.
- Phase 4: native Year in Music now selects 2025 plus 2021–2024 archives on demand, keeps concrete historical release identity, and adds request-free annual identity, artist-evolution, new-releases-from-top-artists, and Discoveries/Missed playlist-snapshot chapters from the loaded aggregate. It explicitly requests only supported historical artwork. Read-only Listen Details, explicit manual MusicBrainz matching, and request-free editing plus explicit generation of top-album collages are implemented; mapping-status reads, arbitrary custom-art search, remaining playlist mutations, playback/content resolution, MusicKit-scoped capture, and offline submit queue remain staged. Spotify-linked playback through a libspot/librespot-family implementation is deferred until the core product is complete and may be investigated only on a separate branch after exact project identity, licensing, Spotify policy, authentication, maintenance, and App Store constraints are audited.

## Next implementation sequence

1. Connected Services is now an authenticated Profile > Account destination backed by `GET /1/user/{username}/services`. It is never constructed for public profiles, waits until the user opens it, scopes its five-minute in-memory cache and request coalescing to the credential, preserves stale service names if an explicit refresh fails, evicts private cache data on authorization failure, and exposes the canonical ListenBrainz management page rather than guessing OAuth or permission detail. The guarded Similar Artists follow-up is also complete: canonical Artist Detail lazily uses the public website/official-client `POST /artist/{mbid}/` payload behind a replaceable app adapter, not the experimental Labs graph or LB Radio. It sends no credential, coalesces exact reads, previews five of at most 18 ranked rows with local expansion, mirrors the server's 120-second cache, negatively cools failures/schema drift, and performs no row hydration. Independent review found no remaining material issue; fixture-only light, dark, maximum-accessibility, expanded, and smaller-device checks made no production request. The frozen checkpoint passes 13 focused tests and all 439 app tests with no failures or skips, plus a generic iOS Simulator Release build. Keep the full Music Neighborhood Labs graph, optional Year in Music discovery/social/playlist chapters, and multi-item playlist changes staged until their contracts and incremental value justify the risk.

2. Explicit manual submission is a focused write-only slice: Home and recording actions present the same native `Log a listen` composer, with an ephemeral Playing Now mode. It uses ListenBrainzKit's typed single/Playing Now APIs through the shared mutation gate, sends exactly one POST only after an explicit tap, and never triggers a metadata lookup or post-success poll. A token-free, atomically persisted SHA-256 replay barrier stores only an opaque account-plus-canonical-payload fingerprint and attempt time; confirmed responses and definite failures clear it, while unknown or cancelled-after-dispatch outcomes remain fail-closed until the user explicitly retries the identical frozen payload. Playing Now stays in-memory and has short, account-scoped duplicate suppression because it is temporary. The manual timestamp follows the server's current 2002-10-01 UTC minimum, matches the picker's minute precision, and is capped at now. Editing a prefilled title, artist, or release strips inherited canonical identifiers before submission. Do not add automatic playback capture, batch import, or offline queueing to this surface. Independent correctness and security reviews report ready; fixture-only visual QA covers light, dark, accessibility, smaller-device, success, definite failure, uncertain delivery, and safety-recovery states. The frozen checkpoint passes all 426 app tests with no failures or skips and a generic iOS Simulator Release build.

3. Archived Year in Music is complete for the normalized native story. An explicit year menu selects 2025 or a frozen 2021–2024 report without preloading; archive reads are anonymous, bounded to 16 MiB while receiving and decoding, cached for 24 hours, and cancelled when replaced. The adapter preserves missing metrics, plural legacy artist IDs, concrete 2021–2022 releases, supplied safe 2021 cover art, and year-specific official-art support. The story now also maps presence-aware new-artist discovery, the most-active weekday, noncanonical genre tags, valid release years grouped into decades, optional monthly favorite-artist evolution, and new releases from top artists from the same report. The new-release chapter preserves server order, routes only canonical release-group identities, treats CAA IDs as artwork-only, leaves unmapped/concrete-only rows static, and adds no provider call or hydration. Independent review findings were fixed and re-reviewed. The latest follow-up passes 16 focused Year in Music tests and all 547 app tests, a universal `x86_64`/`arm64` Release simulator build, and fixture-only light/dark/maximum-accessibility/iPhone SE visual checks.

4. Home now presents the account's current pin after its listening snapshot by reusing the same root-owned `PinsModel` as Profile. First appearance is guarded to one current-pin read, repeated Home/Profile tasks join the loaded state, and explicit Home refresh updates only the current pin rather than waking lazy pin history. The response's embedded recording metadata renders directly with no row hydration. Nullable absence, inline failure/retry, dark mode, and iPhone SE layouts are covered by isolated local fixtures whose tab selection is locked to Home. Nine focused pin tests and the complete 552-test app suite pass; the Release simulator binary contains both `arm64` and `x86_64`.

5. Localization now has one translator-facing production string catalog, compiler-backed Release extraction, explicit editable English values, generated-symbol support, and a guarded repository check/sync workflow. The audited production UI uses compiler-extracted SwiftUI literals, `LocalizedStringResource`, or `String(localized:)`; server/user/music content stays verbatim. The catalog contains 1,546 exact production keys with no empty, stale, or missing-English entry, excludes DEBUG fixture copy, and passes an accented small-device checkpoint, a fresh catalog byte comparison, independent source re-review, and the complete 581/581 app suite. Add true catalog plural variants and native-language/RTL QA before shipping the first non-English locale.

6. **Listen elsewhere** now consumes canonical streaming/free-streaming URL relationships already embedded in recent-listen and Playing Now responses. The local Kit decoder tolerates malformed relationship rows while bounding inspected entries and response bytes; the app validates and reconstructs only a small HTTPS provider allowlist, preserves server-first precedence, exposes one action per service, and persists one canonical array with legacy-cache fallback. A single destination remains a direct outside-app action; multiple destinations use one native menu on action surfaces, while the advanced Listen details source section lists each exact service directly. Rendering and opening these actions add zero API, metadata, search, or provider-resolution requests. The checkpoint passes 143/143 Kit tests, 21/21 focused app tests, and all 562 app tests, plus compiler-backed 1,463-key localization verification and fixture-only light/dark/accessibility visual QA; independent security and UI re-reviews report READY. Account linking, content search, and in-app playback remain separate future work.

## Constraints recorded

- Home current pin uses only `GET /1/{user}/pins/current`. A `200` response with a null pinned recording is an intentional empty state, while an initial failure stays inline instead of producing a duplicate global alert. Pin history remains lazy and is requested only by its own destination; Home never hydrates the embedded recording or starts history reads.

- Year in Music **New from top artists** is a discovery list, not a listening ranking. Its card order comes from the annual payload, the model deduplicates release-group identity first and normalized artist/title otherwise, and the view invents no count, rank, date, type, or recommendation explanation. Only `release_group_mbid` creates a destination; `caa_release_mbid` remains artwork-only and legacy `release_mbid` never masquerades as a release group. Rendering adds no ListenBrainz or MusicBrainz read; ordinary visible covers may load lazily from CAA, while the isolated QA fixture disables remote artwork. The checkpoint passes 16 focused and 547 complete app tests, produces a universal Release binary, passes responsive visual QA, and has an independent READY re-review.
- Year in Music **Revisit your year** uses only the current official Top Discoveries and Top Missed Recordings snapshots already embedded in that aggregate. A tolerant Kit decoder accepts direct and nested legacy JSPF plus scalar/array identifiers; the app preserves order and duplicates, discards raw annotation HTML, and routes only strict canonical playlist/recording identities. Cards and the full local detail add no request or artwork load; external ListenBrainz and native Recording Detail are explicit actions. Package statistics tests pass 29/29, focused report tests pass 18/18, the pre-final-UI full regression bundle passes 549/549, subsequent final Debug builds pass, responsive light/dark/maximum-accessibility/iPhone-SE checks pass, and independent re-review reports READY.
- Visited-user **Defining artists** cards now reuse the existing native Artist Detail destination only when the statistics row carries a canonical MusicBrainz artist ID. Unmapped rows remain readable and noninteractive, and another listener's ranking count is deliberately removed from the route value so Artist Detail cannot call it “your listens.” Destination construction performs no hydration or network request; the existing lazy profile-stat request remains the only request needed to render the section. Normal sizes retain the artwork-first horizontal shelf, while accessibility sizes use full-width rows. The checkpoint passes all 517 app tests, a universal `x86_64`/`arm64` Release simulator build, fixture-only light/dark/maximum-Dynamic-Type/iPhone SE visual QA, and an independent READY review.
- Taste artist rankings, Artist Evolution, and Year in Music now apply that same canonical-identity rule consistently. Rows and cards with an artist MBID retain the existing Artist Detail route, disclosure affordance, and concise VoiceOver hint; unmapped artists remain fully readable without implying a destination. The gate is an in-memory model decision, adds no request or row hydration, and preserves the single aggregate request already owned by each surface. Fixture-only mixed-identity QA verified both affordance states, all 517 app tests pass, the universal `x86_64`/`arm64` Release simulator build succeeds, and independent review reports READY.
- Visited-user profiles now add **Most played albums** as an independently lazy all-time section. Opening a profile still performs only its three overview reads; the album section starts at most one six-item aggregate release-stat read when it appears, uses the existing process gate and five-minute username-plus-credential cache, never hydrates rows, preserves stale values, and refreshes only a section that was already loaded. A refresh overlapping the initial album request now joins the active work instead of invalidating it or starting a second request; the same correction hardens Defining Artists. Canonical release MBIDs reuse Release Detail, while unmapped rows stay static without disclosure or a VoiceOver navigation hint. Native rows adapt to small screens and accessibility text sizes. Focused coverage includes the suspended-request race, fresh-cache zero-call behavior, cache-field preservation, and canonical routing; all 523 app tests pass, the universal `x86_64`/`arm64` Release simulator build succeeds, fixture-only light/dark/maximum-Dynamic-Type/iPhone SE QA passes, and independent re-review reports READY.
- Taste Rankings now includes a distinct **Release groups** view without conflating an album work with a concrete release. It reuses ListenBrainzKit's existing all-time aggregate endpoint only after the user selects that ranking, caches it for ten minutes by normalized username and credential scope, never hydrates rows, and keeps stale results visible. A model-owned flight lets selection changes, pull-to-refresh, and explicit refresh join the same intentional read; failed states retry only after a tap, and sign-out cancels retained work. Canonical release-group MBIDs reuse Release Group Detail, while unmapped rows remain static. The existing Taste period-pill pattern replaces the cramped segmented ranking control, and the new rows expand at accessibility sizes. Seven direct lifecycle tests, all 530 app tests, a universal `x86_64`/`arm64` Release build, fixture-only light/dark/maximum-Dynamic-Type/iPhone SE checks, and independent re-review pass with no production request.
- **Most played tracks** now appears in the signed-in Profile by reusing the already-loaded all-time rankings, with no new request or load path. Visited profiles add the same presentation as an independently lazy six-item aggregate read, cached for five minutes by normalized username and credential scope. Stale rows remain visible with a saved-data notice; failures retry only after the visible action or an intentional pull-to-refresh, and a refresh overlapping the first request joins it rather than duplicating or discarding it. Track detail navigation requires a canonical recording MBID; a release ID or display strings never make an unmapped track interactive. The local no-artwork fixture includes mapped, unmapped, and long-title rows. The final checkpoint passes 23 focused lifecycle/cache tests and all 537 app tests, produces a universal `x86_64`/`arm64` Release simulator binary, passes light/dark/maximum-Dynamic-Type/iPhone SE visual QA, and has an independent READY re-review; fixture log inspection found no ListenBrainz, MusicBrainz, Cover Art Archive, or Archive.org host access.
- Home now closes the remaining top-track gap with a compact artwork-first **Most played tracks** shelf backed only by the already-loaded `ListeningSnapshot.topRecordings`. It adds no provider call, cache, refresh path, or row hydration. Canonical recording MBIDs reuse Track Detail; release identity and display strings never make an unmapped ranking interactive. Normal text sizes use a horizontally aligned media shelf, while accessibility sizes switch to full-width rows with unbounded labels and mapped-only disclosure affordances. The isolated mapped/unmapped/long-title fixture disables remote artwork. The checkpoint passes all 537 app tests, produces a universal `x86_64`/`arm64` Release simulator binary, passes light/dark/maximum-Dynamic-Type/iPhone SE visual QA, and has an independent READY review; scoped fixture logs contain no service-host activity.
- Ranked tracks now follow the same canonical recording-identity rule across Home, signed-in and visited profiles, Taste Rankings, Artist Detail, and Year in Music. A recording MBID is the only identity that enables Track Detail; release MBIDs and display strings never substitute. Unmapped rows remain readable without a chevron or VoiceOver navigation hint, while mapped rows retain both. The correction adds no hydration or provider read, and isolated mixed-identity fixtures make no live network request. Adaptive rows preserve full titles, artists, and counts at maximum Dynamic Type, including a capped scalable Year in Music rank badge. The checkpoint passes 10 focused Year in Music tests and all 537 app tests, produces a universal `x86_64`/`arm64` Release simulator binary, and passes light/dark/maximum-Dynamic-Type/iPhone SE visual QA; scoped fixture logs contain no service hostname.
- The configured in-app browser was unavailable. A temporary isolated Playwright/Chromium setup was therefore used to inspect the live mobile 2025 Year in Music page in addition to current frontend source/routes; all temporary packages and screenshots are recorded in the cleanup log.
- Xcode 27, the iOS 27 runtime, app build, test bundle, real simulator tests, and live public data have now been exercised. Visual checkpoints cover onboarding plus real-data Home in light/dark mode; smaller-device validation is recorded with the build evidence.
- The current plus archived Year in Music checkpoint passes 137 vendored-package tests and all 440 app tests, with a successful Release simulator build. The focused report/artwork suites pass 15 tests. Fixture-only simulator checks cover 2021 and 2024, concrete historical releases, current 2025 regression, official artwork, light/dark mode, maximum accessibility text, and the smaller iPhone SE layout without a production request.
- Year in Music is an on-demand Taste destination for the stable 2025 report and frozen 2021–2024 archives. It performs exactly one process-gated aggregate request for the selected year, never preloads archives or hydrates rows, caches the authenticated current report for 30 minutes and anonymous archives for 24 hours, preserves stale content on refresh failure, and treats 204, 404, or empty report data as unavailable. The native story maps totals, UTC listening days, presence-aware new artists, canonical weekday context, display-only genre tags, valid release-year decades, optional monthly favorite-artist evolution, top artists, release groups or concrete historical releases, and recordings; supplied 2021 cover art is preferred without collapsing release and release-group identity. Its MPL-covered Cassette adaptation provides the mesh hero and ranking composition, while the annual heatmap and identity chapter are independently implemented; evolution reuses only the app's render-only chart pieces. Supported official art remains a separate explicit action through the bounded shared renderer; 2021 deliberately offers report-link sharing only. The current checkpoint passes 14 focused and 545 full app tests, universal Release verification, responsive fixture QA, and an independent READY re-review without a production request.
- Generic Art now exposes explicit statistics artwork from Taste, artist cover grids from canonical Artist pages, and authenticated playlist artwork from Playlist Detail. Public requests use an empty-token client and anonymous cache scope; playlist requests remain credential-scoped. Exact reads coalesce in the process-wide gate, never auto-retry, cache content or expected absence for up to 24 hours, and stay under a 16 MiB total SVG budget. The shared nonpersistent, JavaScript-free WebKit renderer preflights every resource, blocks all other traffic, admits only current ListenBrainz placeholder, Archive/CDN cover, and exact Google font shapes, then exports a sanitized UUID-isolated PNG. Dismissal cancels Generic Art and Year in Music retries. The checkpoint passes 135 vendored-package tests and all 376 app tests plus a successful Release simulator build; focused Generic Art coverage includes typed request paths/options/bounds, response validation, cache/scope isolation, coalescing/drain behavior, lifecycle cancellation, URL policy, and WebKit rule compilation. Local light/dark/accessibility/unavailable/failure fixtures and one explicit token-free public artist request visually verified real redirected covers, the placeholder, high-resolution capture, and the enabled share action. Independent correctness and security reviews found no P0–P2 issue.
- **Album collage** now turns already-loaded all-time top releases into an ordered, native art composer without making a request while the user selects, rearranges, or styles covers. Its draft type carries no remote artwork URL, accepts only canonical release MBIDs, and exposes only exact server-supported layouts. **Create** produces one anonymous, coalesced `POST /1/art/grid/`; failures never auto-retry, and preview, bounded SVG validation, exact-request caching, PNG export, and sharing reuse the hardened Generic Art path. The checkpoint passes 145/145 vendored-package tests and all 581 app tests, compiler-verifies the sole 1,546-key catalog, produces a universal `x86_64`/`arm64` Release simulator binary, and passes fixture-only light/dark/maximum-Dynamic-Type/iPhone SE composer plus rendered-preview checks. Scoped fixture logs contain no service-host activity, and independent correctness and security re-reviews report READY.
- LB Radio now exposes explicit authenticated generation from listening history, unheard recommendations, artist/tag seeds, or an advanced Troi prompt. Each generator and optional batch metadata operation independently enters the process-wide gate; generator 429 responses remain visible, metadata failures preserve the JSPF mix, and metadata 429 timing still defers later work without regenerating. Results reuse the playlist mosaic/row hierarchy, navigate only canonical recordings, and state that playback depends on later legal content resolution. The checkpoint passes 114 vendored-package tests and 192 app tests plus a Release simulator build; light, dark, accessibility-extra-large, and smaller-device fixture QA made no ListenBrainz request and used no account token.
- Playlist creation and metadata editing now use typed exact-JSPF ListenBrainzKit requests and independently written native SwiftUI. Creates default private and contain no tracks; owner edits preserve the full title, annotation, privacy, and collaborator snapshot because the server overwrites supplied state. Each user gesture is serialized and admitted once through the shared request gate, with no automatic retry because the endpoints expose no idempotency key. Edit metadata is force-refreshed before opening and saving, changed server snapshots abort rather than overwrite, and ambiguous responses—including cancellation after transport begins—disable another save pending reconciliation. Successful/indeterminate mutations evict relevant profile caches; confirmed edits evict every detail access scope before restoring authenticated state and update already-loaded Profile rows through a token-free journal. The checkpoint passes 118 vendored-package tests and 210 app tests; independent correctness and security reviews report no remaining material issue. Light, dark, accessibility-extra-extra-large, and smaller-device fixture QA made no ListenBrainz request and used no real token.
- Recording Detail now appends one canonical recording MBID to an owned or collaborating playlist through a searchable Minidisc-derived native sheet. A fresh detail preflight detects existing occurrences and requires explicit consent for another copy; the duplicate baseline is refreshed again after a delayed confirmation. Reads enter the bounded/coalescing lane and the one POST enters the serialized mutation lane; the mutation is never retried, and post-transport uncertainty is reconciled by occurrence count before another attempt is allowed. Because the API has no conditional append or item-version token, an external collaborator can still race the best-effort preflight; the UI never claims stronger atomic duplicate prevention, and an occurrence increase after an ambiguous response remains indeterminate. Authentication/visibility failures purge cached and visible private playlist data while ordinary offline failures may retain stale content. The checkpoint passes 119 vendored-package tests and 224 app tests plus a Release simulator build; independent correctness and security reviews report no remaining material finding. Fixture QA covers light, dark, accessibility-extra-extra-large, and smaller-device layouts without a production request or real token.
- Playlist Detail now duplicates any authenticated-visible playlist after a forced source refresh and a native confirmation that describes server-current visibility inheritance without promising an atomic snapshot. The typed empty-body POST enters the process-wide gate exactly once and is never retried. A token-free account/source attempt record is persisted before dispatch; the returned MBID is persisted immediately and then fetched canonically before its exact title/privacy enters navigation or an already-loaded Owned list. Canonical confirmation requires the returned UUID, normalized owner, and `copied_from` provenance. Lost responses cannot be cleared by stale on-screen data or a relaunch: **Check Again** performs a fresh Owned request, accepts only recent source provenance, fetches that destination canonically, and keeps the barrier when absence cannot prove failure. A confirmed record remains until the user sees and acknowledges the exact destination. Authentication or visibility loss clears caches before publishing an ordered journal event, redacts current detail, invalidates older Profile responses, and resets affected pagination before already-rendered rows reconcile. The checkpoint passes 120 Kit tests and 248 app tests plus a Release simulator build; both independent correctness and security re-reviews report ready. Fixture-only light, dark, accessibility-extra-extra-large, and smaller-device checks made no production request and exposed then corrected a clipped large-text confirmation.
- Playlist Detail now removes exactly one visible track through the current positional item-delete endpoint. A fresh, non-coalesced canonical inspection must still authorize the authenticated owner/collaborator and match the displayed track at its exact zero-based wire position before one `count = 1` POST is admitted. A token-free SHA-256(normalized-account)+playlist safety record is durably written before dispatch and never contains a credential, title, or track metadata. It persists across relaunches, is isolated per account/playlist, blocks after a lost response, mismatch, refresh failure, or durable-clear failure, and permits pair-scoped reset only after a fresh inspection and explicit confirmation. Successful state requires the canonical result to equal the inspected playlist minus precisely the selected slot; it never claims success from a response alone. Invalid authentication, missing playlists, and read-confirmed visibility loss redact rendered private detail and every detail/list cache scope; a mutation-only permission rejection first performs a fresh read and preserves a visible playlist as read-only. The server exposes no playlist revision or conditional positional delete, so a collaborator can still reorder tracks between inspection and POST; exact postflight reconciliation prevents a false success claim but cannot undo a wrong server-side positional deletion. The local MPL Kit extension and official KMP endpoint shape were compared; Swift uses the smaller Kit extension while retaining KMP as an optional future domain source. The final checkpoint passes 122 Kit tests and the complete 316-test app suite, including 18 focused removal-model tests, plus a generic Release simulator build. Fixture-only light/dark/small/accessibility visual QA made no live request or mutation.
- Phase 3 statistics now includes on-demand server activity, daily/hour activity, and release-era activity for the website's seven primary ranges. The daily response is normalized into a selectable 7×24 UTC grid; era data becomes a categorical decade chart that drills into the ten constituent years. Both are cached per normalized user and period and retain stale data through refresh failure. Era normalization combines duplicate years, rejects malformed non-four-digit years, bounds gap filling, and saturates count overflow. Explicit unavailable/empty/retry states, VoiceOver chart descriptors/cell values, and horizontal accessibility-size expansion keep presentation honest. Fixture-only visual checks covered decade and year views in dark/light mode and accessibility-extra-large text without a production request.
- Artist Evolution is a deliberate drill-in rather than an eager Taste request. One shared-gate request returns the server-ranked artists for the selected range; unordered sparse rows are regrouped by stable MBID/name identity, duplicate and negative counts are defended, missing buckets are zero-filled, and all-time years are bounded. A cached stale-while-revalidate model preserves useful results, while native multi-series lines, a top-artist selector, touch-selected time breakdown, artist navigation, and a VoiceOver chart descriptor replace the website's hover-dependent stream graph. Fixture-only light, dark, and accessibility-size QA made no production request.
- Genre Activity is an on-demand Taste drill-in backed by one shared-gate aggregate request and a 10-minute per-user/period cache. The server returns only its top genres per UTC hour, so the native UI deliberately avoids percentages and calls the values overlapping genre-tag matches rather than a complete distribution. Free-text case/whitespace variants coalesce without inventing stable genre identity; invalid hours, negative counts, and overflow are bounded. UTC buckets use their midpoint plus the report-end time-zone offset to form Night/Morning/Afternoon/Evening, with the daylight-saving approximation explained in the interface. Fixture-only light, dark, lower-section, and accessibility-size QA made no production request. The completed checkpoint passes 101 vendored-package tests and 167 app tests, with a successful Release simulator build.
- Artist Origins is an on-demand Taste drill-in backed by one public aggregate response and a 10-minute cache keyed by normalized user, credential scope, and server range. The local Kit extension supports user and sitewide shapes, honest 204, tolerant legacy counts, and optional embedded artist MBIDs. The app merges duplicate country/artist rows, clamps invalid counts, preserves malformed country data as an explicit unknown bucket, and uses the response's embedded artists rather than fan-out hydration. Artists/Listens ranking, proportional bars, and country sheets are local interactions; only changing period can select a different exact read key. The first native slice deliberately favors an accessible ranked list over copied GPL GeoJSON or first.fm's dated coordinate table. Fixture-only light/dark/accessibility/country-detail/smaller-device QA made no production request and used no real token. The final checkpoint passes 125 Kit tests and the complete 337-test app suite plus a generic Release simulator build; independent review reports no material request-traffic, correctness, or accessibility finding.
- Artist Activity is an on-demand Taste drill-in backed by one aggregate response and a 10-minute cache keyed by normalized user, credential scope, and server range. The local Kit extension supports user and sitewide shapes, credited and canonical artist names, optional stable artist/release-group identities, tolerant counts, and honest 204. The app merges MBID-first duplicate artist and album rows, bounds invalid counts and overflow, and uses only the response's embedded entity identifiers. Ranking, album composition, and expand/collapse are local interactions; opening a canonical artist or release group intentionally uses the existing detail flow. Concurrent consumers join one flight, quick destination reopen does not duplicate it, and session teardown cancels active work so queued post-logout traffic cannot linger. Fixture-only light/dark/accessibility/expanded/smaller-device QA made no production request and used no real token. The final checkpoint passes 127 Kit tests and the complete 343-test app suite plus a generic Release simulator build; independent review reports no material request-traffic, correctness, copy, or accessibility finding.
- Canonical Artist and Release Group details now include a native all-time Top Listeners summary. The local Kit extension preserves the current endpoint's artist/release-group identity, names, artist credits, Cover Art Archive context, aggregate counts, bounded ranked rows, and honest 204. Each detail performs at most one exact shared-gate read, caches content and unavailable results for five minutes within the credential scope, never hydrates listener rows, and expands the server's at-most-10 sample locally. Stale rankings remain visible with an explicit refresh notice; retry work cancels when the view leaves. Fixture-only light/dark/expanded/accessibility/smaller-device QA made no production request or real-token read. The final checkpoint passes 129 Kit tests and the complete 350-test app suite plus a generic Release simulator build; independent review reports READY after request-hygiene, model, lifecycle, navigation, copy, and accessibility fixes.
- Discover now includes a native Following Pins destination backed by the public aggregate endpoint. The local Kit extension keeps its response distinct from pin history, tolerantly drops malformed rows, preserves count/offset/user metadata, and maps invalid-query/not-found responses. The app performs one anonymous shared-gate call per page, bounds wire parameters and mapped rows, caches exact pages for five minutes, advances by the server count, deduplicates by normalized owner plus row ID, preserves stale content on refresh failure, and cancels or rejects abandoned/late work. Cards use only embedded metadata, open existing recording/user destinations, and never hydrate rows or expose owner mutations. The checkpoint passes 130 Kit tests and the complete 358-test app suite plus a generic Release simulator build; independent review reports READY. Fixture-only light/dark/accessibility/smaller-device/empty/failure QA made no production request and used no real token.
- Entity popularity now uses the four official batch endpoints for artist, recording, concrete release, and release group. The app requests only a canonical detail entity, never hydrates rows, preserves missing counts as unavailable rather than zero, coalesces duplicate in-flight work, and keeps a bounded 24-hour cache because the dataset is rebuilt daily. Global listens/listeners remain visually and semantically separate from personal listens. Transport enters the process-wide bounded/coalescing read lane; a separately paced public smoke test covers the live batch endpoint. The UI is independently implemented from inspected first.fm and current ListenBrainz patterns, and network-free fixtures cover light, dark, accessibility, and smaller-device layouts. The completed checkpoint passes 105 vendored-package tests and 174 app tests, plus a successful Release simulator build.
- Fresh Releases now uses an upstreamable ListenBrainzKit extension. Personalized results are the default and an HTTP 204 becomes an honest empty state; selecting All is the only route that makes a sitewide request. A normalized exact query owns scope, the endpoint-supported 7/30/90-day window, past/upcoming inclusion, and server ordering; sitewide requests fail closed to their narrower supported shape. Matching loads coalesce in the model and shared request gate. Sort direction plus inclusive/exclusive release-type and tag filters operate only on the loaded aggregate, with no row hydration or additional request. The UI preserves concrete release versus release-group identity, adapts to maximum Dynamic Type, and keeps the sitewide endpoint's required terminal slash under transport tests.
- Search now exposes Users, Artists, Albums (release groups), Tracks, and public Playlists in one native sheet. It sends only the selected scope after a 500 ms debounce, caches per normalized query, cancels abandoned/stale work, preserves release-group identity, and reuses the native artist/recording destinations. ListenBrainz and MusicBrainz have independent process-shared gates; canonical paths avoid hidden redirect requests. Real MusicBrainz results were visually checked in dark/light mode and XXL Dynamic Type.
- Pins now includes a current-profile card, lazy paginated history, recording-detail pin/unpin, 280-character notes, and owner-only edit/delete actions. Mutations are optimistic with rollback, and the ListenBrainzKit extension follows the endpoint-specific response contracts (including the Boolean update result). Public-profile empty-state presentation was visually checked in the simulator; production mutations were deliberately not exercised.
- User search now opens a native visited-user profile with identity and listen count, latest/Playing Now context, current pin, recent listens, and a lazy all-time artist shelf. Reads are staged instead of using Android's eager profile waterfall. A five-minute, 100-entry LRU cache tracks overview and artist freshness separately so stale content remains visible while only the expired section revalidates. Dark-mode real-data simulator QA covered missing artwork and the finite request sequence.
- Visited-user profiles now link to a lazy native Social destination with complete follower/following lists, similar listeners, pairwise compatibility, and authenticated follow state. Public graph data and viewer-specific relationship data use separate five-minute bounded caches. Follow/unfollow is serialized, optimistic, and rollback-safe; rows intentionally avoid profile hydration and N+1 requests. Real public data was visually checked in dark mode and at accessibility-extra-large Dynamic Type; production mutations were deliberately not exercised.
- Album search now opens a native release-group page enriched by one ListenBrainz metadata request: artwork, credited artists, tags, dates/types, and an explicit release-group identity. The endpoint's misleading `release` inclusion mirrors group metadata, so it is not presented as a concrete edition. Playlist search now opens a complete JSPF-backed page with an artwork mosaic, creator, visibility/dates, duration, and a lazy ordered track list; a 289-track production playlist rendered from one detail response with no row hydration. Both use five-minute bounded UUID caches and preserve stale or search-seed content through refresh failures.
- For You now presents paginated collaborative-filter tracks and server-generated Daily Jams, Weekly Jams, and exploration playlists. A nonempty track page costs exactly one CF request plus one batch `/1/metadata/recording/` request through the shared gate; empty pages skip hydration, playlist reads remain lazy, and rows reuse the canonical recording/playlist destinations. Real data was checked in dark/light mode and at an accessibility text size.
- Profile now exposes owned and collaborating playlist categories as independent lazy pages. Each page is one metadata-only shared-gate request; opening a playlist is the only action that fetches tracks. Public profiles remain usable without a token, while cache keys separate public data from each authenticated viewer so private rows cannot cross sessions. Server count/offset/total metadata drives pagination, stale appended pages never advance the cursor before revalidation, and failed pages retry the exact offset. Fixture-only QA covered both categories, light/dark mode, malformed identifiers, and accessibility-extra-extra-large text.
- Feed now presents My Feed plus distinct Following and Similar listening modes. Every page is one shared-gate request with embedded metadata and no per-card hydration; pagination uses the exact oldest timestamp, stable actor-aware deduplication, and a fixed seven-day lower bound for listening modes. Unknown events survive decoding, malformed nested track metadata degrades to a generic event, and hidden events reveal neither actor nor type. Authenticated-gate and DEBUG fixture visual QA covered dark/light mode and accessibility-extra-large text; a production private-feed request was deliberately not claimed without an authenticated QA account.
- Recommendation feedback is kept distinct from recording Love/Hate. An authenticated For You page performs one batched feedback lookup for its MBIDs after the recommendation and metadata reads, with every request entering the process-wide gate. Hate, Dislike, Like, and Love mirror the current website; the server's extra `bad_recommendation` value remains typed in ListenBrainzKit but is intentionally not surfaced. Mutations optimistically update, disable that row while pending, tap again to clear, and roll back with a user-facing error. No authenticated production mutation was exercised without a disposable QA account.
- Recording detail now offers public follower recommendations and a native multi-select personal-recommendation sheet. Eligible recipients come from one cached followers request, local search causes no additional calls, notes are limited to 280 characters, duplicate submissions serialize, failures preserve the user's selection, and successful sends invalidate feed caches without forcing a refresh. The selection interaction follows Cassette's MPL-compatible sheet pattern but was independently implemented with app-owned types; official clients supplied behavior only.
- My Feed now exposes only the mutations accepted by the current server: thanks for another listener's recommendation/pin, hide/unhide for supported foreign events and owned notifications, generic deletion for owned recommendation/notification events, and the dedicated Pins deletion route for owned pins. Hidden cards intentionally reveal neither actor nor event type. Mutations use stable server row IDs, optimistic hide/delete with rollback, per-event serialization, shared-gate transport, and cache invalidation without an automatic follow-up request. No authenticated production mutation was exercised.
- Concrete release pages now use one independently gated MusicBrainz lookup with `artist-credits+recordings+media+release-groups+labels`, then render position-normalized discs and tracks without row hydration. A concrete release MBID is the canonical navigation identity from Fresh Releases, Home, Taste, Profile, and Recording Detail; release groups remain a separate native destination. Fresh Releases tags/listen context survives the shared navigation path, stale cached editions survive refresh failures, and unmapped tracks remain visible but non-navigable.
- History date jumps issue one bounded ListenBrainz request for the selected local calendar day, using strict `min_ts = start - 1 second` and `max_ts = next local day` semantics that remain correct across DST. Selected-day results are transient and never replace the cached recent snapshot; pagination preserves the lower bound, overlaps same-second cursors, retains distinct MSIDs, terminates repeated cursors, and cancels superseded day selections. Native History search is deliberately local: it matches canonical and submitted artist, release, and track metadata only among already-loaded latest/day listens, filters the latest Playing Now section consistently, and performs no ListenBrainz/MusicBrainz provider read, metadata hydration, refresh, or pagination. A stable raw-tail sentinel prevents query changes from recreating a paging task; active search also suppresses paging errors and footers. Clearing restores the ordinary history surface, while newly visible rows retain normal artwork loading. Server-wide artist/source/search filters remain unsupported. The checkpoint passes all 494 app tests and the unchanged 138-test vendored-package baseline plus a universal arm64/x86_64 Release simulator build; independent correctness and request-safety reviews report READY.
- History now exposes authenticated single-listen deletion only for non-Playing-Now rows with an MSID. The existing ListenBrainzKit timestamp/MSID transport is protected by canonical token-owner validation, a synchronized atomic Application Support journal, cross-scene reservation, and confirmed/indeterminate replay barriers. Accepted rows disappear from live and restored caches; uncertain outcomes remain visible and require a separately warned retry. The checkpoint passes 121 Kit tests and 266 app tests plus a universal Release simulator build. Independent correctness and security re-reviews report ready. Fixture-only checks cover light, dark, smaller-device, long-title, accessibility-extra-extra-extra-large, and recovery-alert presentation without a production mutation or real token.
- Simulator QA covers recording sharing in light/dark mode and accessibility-extra-large text, the hidden-feed privacy state, and concrete editions at both the artwork/facts and ordered-track positions. Edition QA exposed and fixed a two-column accessibility-facts squeeze and moved durations below titles at accessibility sizes. An earlier checkpoint also exposed and fixed a missing `PinsModel` environment value when recording detail is presented from the bottom accessory.
- Runtime QA exposed three transport/metadata defects that source-only tests had missed: release-group and recording-metadata routes require terminal slashes to avoid hidden redirects, encoded JSON bodies need an explicit content type, and Cover Art Archive rejects uppercase UUID paths. Canonical request/header tests and centralized lowercase artwork URL tests now guard the fixes.
- Account and cache isolation now bind every account-sensitive in-memory key to a process-local HMAC credential scope. Viewer-specific social state also includes normalized viewer and target identity. Authenticated username/token persistence is atomic in Keychain, legacy raw-token records validate and migrate before activation, and sign-in, public browsing, and sign-out purge username-keyed disk snapshots before changing observable identity. Independent correctness and security re-reviews report ready; the integrated suite passes 294 tests, including deterministic queued-read cancellation coverage.
- Official KMP framework export was attempted and currently fails at the native Room KSP step; it remains a behavior reference rather than an app dependency.

## External source links

- Recording Detail, History/Playing Now context menus, and Listen details now expose only verified external destinations already present in a listen payload. The app prefers a valid Spotify track ID, otherwise accepts strict HTTPS Spotify, YouTube, SoundCloud, Apple Music, Internet Archive, or Bandcamp origins; arbitrary hosts, raw media URLs, schemes, user info, nonstandard ports, and tracking parameters are rejected or removed. This is a zero-request bridge, not playback, search, or content resolution. Playback SDKs and Spotify account support remain staged. The completed checkpoint passes 13 focused link/cache tests and the full 446-test app suite plus a universal Release simulator build. Independent correctness and security re-reviews report ready. Fixture-only QA covers light, dark, maximum accessibility text, and the smaller iPhone SE layout without a production request or real token.

## Artist highlights

- Artist Detail now reuses the combined public artist-page payload already fetched for Similar Artists. The current website and official KMP client establish this behavior; the native app bounds and decodes its 10 popular recordings, 10 highest-ranked release groups, and 18 similar artists through one guarded adapter, then switches Tracks/Releases and expands the first five locally with no additional request or row hydration. A shared 120-second positive/negative cache and exact request coalescing prevent independently mounted sections from duplicating traffic. The documented ranked popularity GETs remain a ListenBrainzKit gap because the current server makes them token-protected and unbounded. The checkpoint passes 24 focused tests and the complete 457-test app suite plus a universal Release simulator build. Fixture-only QA covers Tracks/Releases, loading/failure, light/dark, maximum accessibility text, and the smaller iPhone SE; independent correctness, security, and UX/accessibility reviews report READY.
- Playlist Detail now offers permanent deletion only to the exact authenticated creator. A fresh non-coalesced owner read precedes one serialized empty-body POST; a protected, token-free account/playlist digest barrier is synced before admission and retained across any ambiguous response or relaunch. Success and verified absence evict detail/profile caches before clearing the barrier and synchronously remove the row across screens; `403`, access loss, 429, cancellation, corrupt storage, and stale in-flight Profile pages have dedicated fail-closed coverage. The checkpoint passes 138 Kit tests, a 54-test focused deletion/Profile suite, and all 490 app tests, plus a universal Release simulator build. Local-only visual QA covers confirmation, recovery, confirmed absence, light/dark, maximum Dynamic Type, and iPhone SE; independent correctness and security reviews report READY.
- Playlist Detail now supports one deliberate track reorder for an owner or collaborator. Opening, dragging, resetting, and cancelling are local and make no request. **Save order** performs one fresh non-coalesced full-order read, one serialized non-retrying move POST, and one fresh exact postflight read on the normal path. Stable slot fingerprints use recording MBID, added time, and normalized contributor; playlist identity and every canonical position must match. Removal and reorder share a protected token-free account/playlist barrier, including migration of legacy removal records, so an ambiguous non-atomic delete-then-add result cannot be replayed across scenes or relaunches. Definite `400`/`429` responses skip postflight, `403` gets one identity-checked visibility read, access loss purges private state, and every other ambiguous live result gets at most one postflight; post-admission cancellation leaves recovery blocked. The checkpoint passes 139 Kit tests, a 61-test focused suite, all 516 app tests, and local small-screen/dark/accessibility/recovery visual QA. Independent correctness and security reviews report READY.

## Artist page context consolidation

- Reuse the same guarded artist-page response for the artwork-first hero, bounded identity metadata, all-time community totals, and the server-ranked top-listener sample. This replaces the page's separate authenticated popularity and entity-listener reads; those typed ListenBrainzKit providers remain available to other entity pages and explicit standalone refresh flows. Decode the coalesced transport into one typed context once, tolerate absent optional fields, reject mismatched entity identity, cap the embedded SVG/listener collections, and never fan out to hidden fallback requests. Render official cover SVG only through the existing nonpersistent, JavaScript-free, allowlisted renderer, falling back to the app-owned artist artwork. Adapt the existing popularity and Top Listeners components rather than creating parallel UI.
- Completed: Artist Detail now makes one anonymous combined read for identity, cover art, global totals, top listeners, popular recordings/releases, and similar artists. The typed response is decoded once inside the coalesced operation; success, absence, and failure are published to the 120-second cache before its request key drains, so independently mounted sections cannot create a late duplicate or hide the page-owned retry action. The hero retains its native fallback until a completed secure SVG snapshot is available. Fixture-only light/dark/accessibility/failure/smaller-device QA made no production request or used-token read; correctness, security, and UX/accessibility reviews report READY. The checkpoint passes 14 focused tests and the complete 463-test app suite plus a universal Release simulator build.

## Rate-limit behavior decision

- Public API guidance recommends that clients start no more than one request per second and honor server rate-limit timing. This project has direct permission to behave like the official clients provided traffic remains selective, intentional, and free of lifecycle or retry storms.
- The official Android/KMP client maps HTTP 429 but has no global scheduler or reset-header handling. The official iOS client likewise has no pacing/retry interceptor. Their useful precedent is bounded paging, debounce, cache reuse, lifecycle cancellation, and realtime delivery—not unbounded fan-out.
- The bounded/coalescing policy in `request-policy.md` is active. At most two independent reads run concurrently; exact reads coalesce only within an opaque credential scope, final-waiter cancellation removes queued work, and 429 timing defers both lanes without replay. Mutations remain serialized at one start per second and never retry automatically. Closed DEBUG telemetry and deterministic queue/cancellation tests guard against lifecycle, pagination, search, or retry storms without recording request identities or tokens.

## CritiqueBrainz review highlights

- Published-review highlights appear only on canonical Artist, Recording, and Release Group detail pages. Each exact anonymous entity read enters a dedicated process-shared CritiqueBrainz gate (separate from ListenBrainz and MusicBrainz), coalesces identical readers, and spaces distinct request starts by at least one second without automatic retry. The cookie-free/cache-free ephemeral session enforces same-origin HTTPS redirects and a 256 KiB streaming ceiling. A five-minute bounded cache preserves stale visible content on refresh failure. The decoder accepts current flat and documented nested fields but drops hidden/draft, malformed, and non-exact entity rows before cache; ratings use CritiqueBrainz's 1–5 scale, reviewer names come from the `user` object, and an aggregate requires a positive rating count. Cards render review text as plain text, restrict license links to Creative Commons, omit unavailable sections, and cancel reads on dismissal. The full-text reader renders every valid row already present in the existing `limit=5` response and performs zero additional requests; it is neither pagination nor an all-reviews fetch. Review writing, voting, and OAuth remain staged. The reader checkpoint passes all 491 app tests and the unchanged 138-test vendored-package baseline plus a universal arm64/x86_64 Release simulator build; independent correctness, accessibility, and visual reviews report READY.

## Read-only Listen details

- History rows offer a native Listen details sheet backed only by fields already returned in recent-listen and Playing Now payloads. The domain preserves raw submitted names/identifiers, top-level MSID, resolved mapping fields, source/client/service context, and timestamps separately from the canonical display recording. Submitted identifiers and ListenBrainz-resolved identifiers remain visibly separate; status copy reports only that MusicBrainz IDs were submitted unless an actual `mbid_mapping` contains a canonical identifier. Valid UUID identifiers are selectable and link only to canonical HTTPS MusicBrainz entity paths. Origin URLs remain inert text and are reduced to HTTP(S) values without user info, query, or fragment before entering the cache. Old cached Listen snapshots decode without the optional inspection payload. Eligible authenticated historical listens can now open an explicit manual-mapping flow: opening details remains request-free, recording search is user-initiated, debounced, cancellable, and independently MusicBrainz-gated, and confirmation sends one serialized no-retry ListenBrainz POST through the existing Kit. Submitted recording MBIDs retain precedence. The app does not automatically read mapping status, poll, refresh History, or replay an indeterminate save. The completed checkpoint passes 12 focused mapping tests, the full 574-test app suite, and the unchanged 143-test Kit baseline. The compiler-backed generic Release check confirms the sole catalog matches 1,501 production keys. Fixture-only QA covers search and review in light, dark, accessibility text, and the smaller iPhone layout; scoped logs contain no service hostname or fatal failure. Independent correctness and security re-reviews report READY. No live request, credential, or production mutation was used.

## Visited-profile playlists and Year in Music

- Reuse the existing native playlist and annual-story destinations instead of creating profile-specific copies. The visited profile adds two local navigation cards and performs no read merely because those cards are visible. Opening Playlists creates a target-user model that retains the signed-in viewer token for server-authorized visibility while withholding owner-only creation and copy affordances unless viewer and target usernames match. Opening Year in Music passes the visited username as the report subject while retaining the viewer account only as transport scope; report fetch, cache identity, official artwork, report links, sharing, and visible attribution all use that subject. Shared annual copy is concise and neutral so another person's report is never described as the viewer's. Existing request gates, caches, canonical media navigation, and accessibility-responsive layouts remain the only data and presentation paths. The final checkpoint passes 50 focused tests and all 554 app tests with no failure, skip, or runtime warning; a compiler-backed generic Release extraction/build synchronized the sole production catalog to 1,459 keys. Fixture-only light, dark, visitor-privacy, and maximum-Dynamic-Type checks made no live service request. Independent correctness and localization audits report READY.
